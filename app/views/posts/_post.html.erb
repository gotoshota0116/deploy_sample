<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= post.content %>
  </p>

    <p>
    <strong>Imgae:</strong>
    <%= image_tag  post.image.url if post.image? %>
  </p>

</div>
